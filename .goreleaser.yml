version: 2

before:
  hooks:
    - sh -c "cd server && go mod tidy"

builds:
  - main: ./main.go
    dir: ./server
    binary: wicketkeeper

    goos:
      - linux
    goarch:
      - 'amd64'
      - 'arm64'
